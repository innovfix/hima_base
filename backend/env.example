# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=
DB_DATABASE=analytics_demo

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration
PORT=3001
NODE_ENV=development

# SMTP / Email alerts (fill these values before running the monitor)
# Preferred: SMTP_URL (e.g. smtps://user:pass@smtp.example.com)
# Either set SMTP_URL or the host/port/user/pass below.
SMTP_URL=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
ALERT_EMAIL_TO=ops@example.com
ALERT_EMAIL_FROM=no-reply@example.com

# Slack (optional)
SLACK_WEBHOOK_URL=

# Monitor threshold (alert when payments in last hour < THRESHOLD)
PAYMENT_ALERT_THRESHOLD=0
